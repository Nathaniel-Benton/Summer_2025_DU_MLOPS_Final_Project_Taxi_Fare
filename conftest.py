# This file exists so pytest treats the project root as a rootdir and adds
# it to sys.path, allowing test modules in tests/ to `import main` reliably
# regardless of how pytest is invoked (locally or in CI).
